
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  creationTimestamp: null
  name: networkids.firewall.metal-stack.io
spec:
  group: firewall.metal-stack.io
  names:
    kind: NetworkIDS
    listKind: NetworkIDSList
    plural: networkids
    singular: networkids
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: NetworkIDS is the Schema for the networks API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: NetworkIDSSpec defines the desired state of Network
          properties:
            enabled:
              type: boolean
            interval:
              type: string
            statslog:
              type: string
          type: object
        status:
          description: NetworkIDSStatus defines the observed state of Network
          properties:
            IDSstatistics:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              properties:
                stats:
                  additionalProperties:
                    format: int64
                    type: integer
                  type: object
              required:
              - stats
              type: object
            lastRun:
              format: date-time
              type: string
          required:
          - IDSstatistics
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
